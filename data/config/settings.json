{
	# Piracy Reworked - fallback values when LunaLib is not enabled

	# lets rules.csv resolve this mod's rule commands (PiratepatBrokerCMD)
	"ruleCommandPackages":["piratepat"],

	# Master switch. Off = the war chest economy freezes (bases keep running).
	"piratepat_enabled": true,

	# --- War chest economy ---
	# Bases are priced by TIER: baseCost is a tier-2 base, and each tier up or
	# down multiplies by tierCostGrowth (tier 1 ~94k, 2 = 150k, 3 ~240k,
	# 4 ~384k, 5 ~614k at defaults). The pirates decide what to buy by
	# circumstance: below recoveryBases operating bases they rebuild their
	# footing - best tier affordable while reserving enough for another
	# starter; at or above it they save toward a base one tier above their
	# current best. Builds chain as fast as ~10-day checks allow when flush.
	"piratepat_baseCost": 150000,
	"piratepat_tierCostGrowth": 1.6,
	"piratepat_recoveryBases": 2,

	# After a base is destroyed the pirates pause before rebuilding. The pause
	# scales with wealth: a flush underworld regroups fast (their money is the
	# threat), a broke one stays suppressed. Bounds in months; applied live, so
	# refilling the chest also shortens an existing pause.
	"piratepat_rebuildFreezeMinMonths": 1,
	"piratepat_rebuildFreezeMaxMonths": 12,

	# Passive income per operating base per month. Tuned to ~90% of what an
	# average raid costs over an average raid interval, so bases sustain their
	# vanilla raid cadence but passive income alone never buys a new base.
	"piratepat_incomePerBasePerMonth": 2000,

	# Raids: launch cost per fleet point (tier-1 raid ~100 FP, tier-5 ~450 FP),
	# and returns on success (a fraction of the cost recouped as spoils, plus
	# a cut per size point of the raided system's hostile markets).
	"piratepat_raidCostPerFP": 100,
	"piratepat_raidReturnCostFraction": 0.5,
	"piratepat_raidReturnPerMarketSize": 2000,

	# Starting conditions: the chest is seeded to fund this many bases (minus
	# any that already exist in the save) plus an operating reserve.
	"piratepat_seedBases": 2,
	"piratepat_seedReserve": 30000,

	# Hard cap on simultaneously operating (raiding) pirate bases.
	"piratepat_maxBases": 8,

	# Destroying a pirate base offsets your lifetime contribution figure by
	# this much (never below zero) - the infrastructure your money built is
	# ash, and the sector's ledger against you lightens. Also pulls your
	# income share down, helping restore a pierced Piracy Respite.
	"piratepat_baseKillContributionOffset": 150000,
	# destroying ordinary pirate fleets also offsets your contribution, at
	# this many credits per fleet point destroyed (fleet wiped out in a
	# battle you fought in)
	"piratepat_offsetPerPirateFPDestroyed": 300,

	# --- Shipping plunder ---
	# Pirates preying on trade shipping is plunder, and plunder flows to the
	# war chest. Every colony sector-wide is polled for shipping disruptions;
	# the share attributable to pirates (by local pirate strength, or fully
	# if the system carries a pirate-activity condition from a base) is
	# credited to the chest at this rate per point of the raided colony's
	# size. Chest income only - never counts against your contribution.
	"piratepat_plunderEnabled": true,
	"piratepat_plunderPerDisruptionPerSize": 600,

	# --- Underworld tithe ---
	# The underworld collects a monthly cut of the sector's ILLEGAL trade -
	# the same commodity flows vanilla's economy already routes as smuggling
	# fleets. Income that scales with the size of the economy (including every
	# colony you found) instead of with your personal black market habit, so
	# pirates stay financed late game even if you never fence another crate.
	# Counted as sector income, never as your contribution - in fact it
	# steadily dilutes your share of underworld income, helping restore a
	# pierced Piracy Respite when you lay off.
	"piratepat_titheEnabled": true,
	# monthly rate applied to each market's illegal trade value (base
	# prices): illegal-source exports PLUS imports attributed to illegal
	# sources by global export share - both ends of a smuggling run pay,
	# so big consumer colonies (drug/organ demand) count too
	"piratepat_titheRate": 0.25,
	# free ports fence everyone's contraband, and pay accordingly. Applies to
	# ALL free ports - your own colonies' underworld shows as its own intel
	# line, so you can see exactly what your open ports are worth to them.
	"piratepat_titheFreePortMult": 2.0,
	# with ZERO operating bases only this fraction flows (no organization to
	# collect it). Eradication buys years of quiet, but piracy follows
	# civilization: the trickle eventually re-founds a base. 0 = true
	# dormancy as before (only player trade can revive the underworld).
	"piratepat_titheNoBaseFraction": 0.25,

	# --- The broker ---
	# Every underworld-tagged CONTACT offers, alongside their job list, to
	# have things "sourced": commissions for goods no legal market sells -
	# colony-grade industrial equipment and rare blueprints. Deposit paid in
	# full up front, straight into the war chest - and fulfillment is a REAL
	# raid launched by an operating war-chest base against whichever system
	# holds the goods. The broker deals even with zero bases: the deposit then
	# finances the network's reconstruction, and the order waits until the
	# underworld can sail again. Deposits count as personal contribution at
	# full weight, and the raided faction's losses become a receivable in
	# your name worth a cut of the deposit - a raid is loud.
	"piratepat_brokerEnabled": true,
	# commission price as a multiple of the item's base value
	"piratepat_brokerPriceMult": 2.5,
	# The equipment menu is REAL: it lists items actually installed at NPC
	# colonies, a successful commission removes the item from that colony's
	# industry (theft, not manufacture), and the price carries a danger
	# premium scaled to the source's war-sim defenses: this much extra per
	# 100 points of defender + station strength. Sources the network's best
	# base can't crack show greyed out until the underworld grows stronger.
	"piratepat_brokerDefensePremiumPer100": 0.15,
	# open commissions allowed at once
	"piratepat_brokerMaxConcurrent": 2,
	# Trust gate: minimum rep with the CONTACT (not the faction) before the
	# sourcing service is offered - the same personal-rep gate vanilla's
	# contact missions use. One of: NEUTRAL, FAVORABLE, WELCOMING, FRIENDLY,
	# COOPERATIVE (falls back to FAVORABLE if unparseable).
	"piratepat_brokerMinRep": "FAVORABLE",
	# Reach gate: what a contact's network can source scales with the
	# contact's IMPORTANCE (the same stat vanilla uses to gate and scale
	# contact missions). Final commission prices are capped at this base
	# figure doubled per importance level - Very Low 150k, Low 300k,
	# Medium 600k, High 1.2M - and a Very High contact can get anything.
	"piratepat_brokerImportanceCapBase": 150000,
	# Ceiling on rotating blueprint offers per market per month; the actual
	# number offered is 1 + importance level (Very Low 1 ... Very High 5),
	# capped by this.
	"piratepat_brokerBpOffers": 5,
	# fraction of the deposit that becomes debt in your name - the raided
	# faction's losses, bought up by a collection house that knows who paid
	"piratepat_brokerDebtFraction": 0.75,
	# refund fraction when the raid is repelled (the network tried and bled)
	"piratepat_brokerRefundFailed": 0.5,
	# refund fraction when no raid could be mounted at all
	"piratepat_brokerRefundUnserved": 0.9,
	# days the network keeps failing to mount the raid WHILE HAVING an
	# operating base before giving up. With zero bases the order waits
	# instead (up to a year), since the deposit is financing the rebuild.
	"piratepat_brokerStallDays": 60,

	# --- Black market contribution weights ---
	# Sales are valued at actual (demand-adjusted) credits, then weighted by
	# what the goods are worth to a pirate war effort. Purchases are plain
	# revenue. Opposing buys/sells of the same goods at the same submarket are
	# netted against each other, so wash-trading contributes nothing.
	"piratepat_buyWeight": 1.0,
	"piratepat_weaponWeight": 3.0,
	"piratepat_shipWeight": 2.0,
	"piratepat_commodityWeight": 1.0,
	"piratepat_oreWeight": 0.5,
	# Flat bonus when a blueprint is sold on the black market (on top of the
	# vanilla behavior of pirates learning it).
	"piratepat_blueprintBonus": 25000,

	# --- Base garrison ---
	# A defensive fleet stationed on each base, so a solo assault faces a real
	# battle group and not a bare station. Strength scales with tier (FP =
	# base + perTier x tier ordinal, so tier 1 ~40 up to tier 5 ~260). Built
	# with standard pirate generation, so it uses the pirates' roster
	# including any blueprints you've sold them. Re-forms this many days after
	# being destroyed if the base still stands; despawns with the base.
	"piratepat_garrisonEnabled": true,
	"piratepat_garrisonFPBase": 40,
	"piratepat_garrisonFPPerTier": 55,
	"piratepat_garrisonRespawnDays": 30,

	# --- Base defenses ---
	# Scale base defense fleets with base tier (vanilla stations get restored
	# with tier, but defenders stay 2 light + 1 medium patrol forever).
	# Tier 3: +1 medium patrol. Tier 4: +1 medium +1 heavy. Tier 5: +2 medium
	# +2 heavy. Patrol fleet size scales linearly up to the max multiplier at
	# tier 5.
	"piratepat_defenseScaling": true,
	"piratepat_defenseFleetSizeMax": 2.0,

	# --- Base bounties ---
	# Vanilla posts 40k-80k by tier - a spread sized for defenses that never
	# grow. With the garrison and scaling patrols a tier-5 base is several
	# times the fight, so its bounty scales the same way: tier-1 figure plus
	# a step per tier (40/105/170/235/300k at defaults; tier 5 matches
	# vanilla's large Luddic Path base). Off = vanilla's pirateBaseBounty1..5.
	"piratepat_baseBountyScaling": true,
	"piratepat_baseBountyTier1": 40000,
	"piratepat_baseBountyPerTier": 65000,

	# --- Respite piercing ---
	# Piracy Respite ("pirates are wary of attacking you") is vanilla's
	# permanent reward for defeating the pirate colony crisis. Wariness fades
	# when the war chest overflows with YOUR money: once your lifetime
	# contribution and your share of the underworld's income both exceed these
	# thresholds, bases funded by the chest resume preying on your colonies
	# (pirate activity condition: accessibility/stability drain and ambient
	# fleets - never raids). Your share dilutes over time if you stop
	# contributing, so laying off eventually restores the respite.
	"piratepat_respitePiercing": true,
	"piratepat_pierceMinContribution": 500000,
	"piratepat_pierceMinShare": 0.25,

	# --- Debt ---
	# You don't get a price on your head - you get a BILL. The value you fence
	# through a faction's black market becomes a receivable in your name, and
	# an independent collection house buys it up at a discount and comes to
	# collect. One balance, one holder, compounding monthly. There is no
	# pay-off button anywhere: it is settled fleet-to-fleet, in the interaction
	# dialog, when the collector catches you - pay what you're carrying and
	# sign for the rest, or fight. ONE collector fleet, sent at full strength,
	# and the contract lives in its officers: destroy every ship with an
	# officer aboard and the debt dies with them. Let one get away, or run,
	# and the same crew finds you again. Stop paying on terms and the house
	# sells your paper to the pirates, who quote figures they made up. A story
	# point forces them honest, permanently.
	#
	# OFF BY DEFAULT. Safe to switch either way mid-campaign: off, every
	# collector stands down and goes home, nothing accrues, nothing is billed,
	# and any balance already on the books sleeps; back on, that balance wakes
	# with its notice and grace periods restarted from that day.
	"piratepat_debtEnabled": false,
	# What the faction actually lost is the TARIFF it would have collected had
	# the same cargo crossed its docks legally - not the cargo's full value,
	# which was never theirs. Debt accrues at this fraction of the value you
	# fed the pirates. 0.3 matches vanilla's standard 30% tariff. The war
	# chest still receives the full amount; only the receivable is scaled.
	"piratepat_debtTariffFraction": 0.3,
	# Debt only accrues when the market's smuggling suspicion implicates you
	# (the same suspicion level shown in the black market tooltip -
	# transponder-off trading generates none). Below the floor nothing
	# accrues; the attributed fraction scales linearly to 100% at the "full"
	# level (0.3 = "high" suspicion).
	"piratepat_debtSuspicionFloor": 0.05,
	"piratepat_debtSuspicionFull": 0.3,
	# monthly compounding while the collection house holds the paper
	# (0.03 = ~43%/yr - expensive, but survivable)
	"piratepat_debtInterestPerMonth": 0.03,
	# monthly compounding once the pirates hold it (0.06 = ~101%/yr - the
	# balance doubles annually, and they don't send letters)
	"piratepat_debtPirateInterestPerMonth": 0.06,
	# no collector is dispatched below this balance - it isn't worth the fuel.
	# Protects the first hour of a new game.
	"piratepat_debtCollectorMinDebt": 15000,
	# days after the debt opens that the written collection notice arrives.
	# You always learn the debt exists before it can hurt you.
	"piratepat_debtNoticeDays": 15,
	# days after the debt opens before any collector can spawn. Clamped in
	# code to at least the notice period, so lowering it can never get you
	# ambushed before the letter arrives.
	"piratepat_debtGraceDays": 30,
	# spawn roll, per ~10-day check past the grace period. Doubled while the
	# creditors are enforcing after missed payments.
	"piratepat_debtSpawnProb": 0.35,
	# THE collector - there is only ever one, and it is sent at full strength
	# from the first day: no ladder of smaller fleets, no escalation, no
	# second crew while the first is alive. Its fleet points are YOUR combat
	# fleet points times this ratio, never the balance in credits - that is
	# what makes a doomstack structurally impossible. 1.2 is what the old
	# escalation curve only reached at its cap.
	"piratepat_debtCollectorFPRatio": 1.2,
	# Floor on the collector's fleet points - itself clamped in code to your own
	# combat FP, so the floor can never hand a two-frigate player something
	# bigger than they are. (Raising it above your fleet does nothing.)
	"piratepat_debtCollectorMinFP": 12,
	# hard ceiling on the one collector's fleet points - a safety net, not the
	# sizing: the ratio above is what keeps it relative to your fleet
	"piratepat_debtCollectorMaxFP": 400,
	# how long a collector hunts you before giving up and going home. A crew
	# that gives up, or dies to somebody who isn't you, is replaced on the
	# next spawn roll.
	"piratepat_debtCollectorHuntDays": 45,
	# flat burn levels a collector may add so it can actually catch you
	"piratepat_debtCollectorBurnBoost": 2,
	# THE OFFICER RULE's one fork. Off (default): destroy every ship with an
	# officer aboard and the debt dies with the crew, whoever holds the paper.
	# On: destroying the HOUSE's crew makes the house cut its losses and sell
	# your paper to the pirates instead, and it is their crew whose officers
	# have to die - a two-act arc, and the pirate act is reachable by fighting
	# rather than only by defaulting on terms.
	"piratepat_debtSellOnHouseDefeat": false,
	# Once the pirates hold your paper they ALWAYS inflate the figure - never
	# an honest quote. The multiplier is rolled once per collector and frozen,
	# and the number is rounded to something suspiciously round. Your own
	# balance is always printed beside it, so the challenge is never a gamble.
	"piratepat_debtPirateMarkupMin": 1.3,
	"piratepat_debtPirateMarkupMax": 2.2,
	# added to the markup for every comm link you've cut on a collector.
	# Running is allowed and the same crew simply keeps hunting; the only
	# price is on the next lie.
	"piratepat_debtDuckSurcharge": 0.15,
	# story point cost of calling a pirate crew on an invented figure. The
	# audit is permanent for that debt: every later quote is honest.
	# 0 hides the option entirely.
	"piratepat_debtChallengeStoryPoints": 1,
	# Terms: each month you pay the holder's interest PLUS this fraction of
	# the principal, so the balance falls by exactly this much per month
	# whoever holds it, and the plan always terminates (~11 months).
	"piratepat_debtInstallmentFraction": 0.2,
	# floor on a monthly installment, so a small plan finishes instead of
	# dripping forever
	"piratepat_debtInstallmentMin": 3000,
	# miss a payment and this fraction of the SHORTFALL (not the balance) is
	# added back to what you owe
	"piratepat_debtLateFeeFraction": 0.5,
	# consecutive misses before the creditors start enforcing (spawn interval
	# halved, spawn chance doubled) and before they give up and sell
	"piratepat_debtMissesBeforeEnforcement": 2,
	"piratepat_debtMissesBeforeSale": 3,
	# how long an enforcement window lasts
	"piratepat_debtEnforcementDays": 60,
	# reputation points GAINED with the pirates for finishing a collector crew
	# working for the independent house (they appreciate their patron handling
	# the opposition); capped so it can't take you past friendly
	"piratepat_pirateRepPerCollectorKill": 2,
	# reputation points LOST with the pirates for finishing a collector crew
	# working the underworld's OWN paper - once they hold your debt, the
	# collectors you shoot are theirs. Bounded so it can never fall past
	# suspicious. 0 = off.
	"piratepat_pirateRepPerPirateCollectorKill": 1,
	# A debt collector collects BY FORCE. Lose a stand-up fight to one and
	# boarding parties take credits first, then cargo by value, until the
	# balance is covered - and the balance drops by what they took. They never
	# take people, AI cores, or your last supplies and fuel.
	"piratepat_debtSeizeEnabled": true,
	# seized cargo is credited against the balance at this fraction of base
	# value - a hold full of goods is worth less to a collector than cash
	"piratepat_debtSeizeCargoFenceRate": 0.5,
	# units of supplies and fuel a seizure will never touch, so being caught
	# in deep space is a setback and not a soft-lock
	"piratepat_debtSeizeSupplyFloor": 100,
	"piratepat_debtSeizeFuelFloor": 100,
	# whether special items (blueprints, nanoforges, and the like) can be
	# taken. AI cores are always exempt.
	"piratepat_debtSeizeSpecialItems": false,

	# --- Intel ---
	"piratepat_intelShowExact": true,

	# Debug
	"piratepat_debugLogging": false,
}
